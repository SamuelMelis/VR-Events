// Preloads core application data after a user authenticates.
// Extend this function to warm caches or prefetch critical data as needed.
export async function preloadCoreData(): Promise<void> {
  // no-op placeholder
}
